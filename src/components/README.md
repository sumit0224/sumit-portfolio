# Components

Place reusable UI and view components here.

- `ui/`: stateless, reusable primitives.
- `layout/`: structural shell components.
- `sections/`: page section blocks.
- `features/`: feature-scoped smart components.
