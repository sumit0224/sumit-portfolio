# Styles

Global style entry and shared style layers.

- `globals.css`: global imports and style entry.
- `variables.css`: design tokens and CSS variables.
- `animations.css`: shared keyframes/utilities.
